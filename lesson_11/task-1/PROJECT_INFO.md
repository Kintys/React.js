# React Project - Lesson 11

## 📋 Структура проекту

```
lesson_11/
├── src/
│   ├── assets/           # Статичні файли (зображення, шрифти)
│   ├── components/       # Переміщувані компоненти
│   ├── context/          # React Context (ThemeContext)
│   ├── hooks/            # Кастомні хуки
│   ├── layout/           # Компоненти розмітки (Header, Footer, Container)
│   ├── routes/           # Конфігурація роутів
│   ├── store/            # Redux Toolkit store
│   ├── views/            # Сторінки додатку
│   ├── App.jsx           # Головний компонент
│   ├── main.jsx          # Точка входу
│   └── index.css         # Глобальні стилі
├── vite.config.js        # Конфігурація Vite
├── jsconfig.json         # Конфігурація шляхів для IDE
└── package.json
```

## 🔗 Аліаси шляхів

Проект використовує аліаси для зручного імпорту модулів:

| Аліас         | Шлях              |
| ------------- | ----------------- |
| `@`           | `/src`            |
| `@views`      | `/src/views`      |
| `@components` | `/src/components` |
| `@layout`     | `/src/layout`     |
| `@routes`     | `/src/routes`     |
| `@store`      | `/src/store`      |
| `@hooks`      | `/src/hooks`      |
| `@assets`     | `/src/assets`     |
| `@context`    | `/src/context`    |

### Приклади використання:

```jsx
// Замість
import Header from '../../layout/components/Header/Header'

// Використовуйте
import Header from '@layout/components/Header/Header'
```

## 🛣️ Маршрутизація

Проект використовує **React Router v7** (react-router).

### Структура роутів:

```jsx
/ (MasterPage)
├── / (index) - Home
├── /about - About (lazy loaded)
└── /* - 404 Page (lazy loaded)
```

### Конфігурація роутів (`src/routes/router.jsx`):

- **MasterPage** - основний layout з Header, Footer та Outlet
- **Home** - головна сторінка
- **About** - сторінка "Про Нас" (lazy loading)
- **404** - сторінка помилки (lazy loading)

### Навігаційні лінки:

```jsx
import { pageLinks } from '@routes/router'

// Доступні лінки:
pageLinks.home // '/'
pageLinks.about // '/about'
```

## 🎨 Тема (Theme Context)

Проект включає систему зміни теми (світла/темна).

### Використання:

```jsx
import { useTheme } from '@context/theme/useTheme'

function MyComponent() {
    const { theme, toggleTheme } = useTheme()

    return (
        <div>
            <p>Поточна тема: {theme}</p>
            <button onClick={toggleTheme}>Змінити тему</button>
        </div>
    )
}
```

Тема зберігається в `localStorage` та автоматично відновлюється при перезавантаженні.

## 📦 Встановлення залежностей

```bash
npm install
```

### Основні пакети:

- `react` / `react-dom` - React бібліотека
- `react-router` / `react-router-dom` - Маршрутизація
- `@reduxjs/toolkit` / `react-redux` - Управління станом
- `vite` - Build tool

## 🚀 Запуск проекту

```bash
# Режим розробки
npm run dev

# Збірка для продакшн
npm run build

# Перегляд production збірки
npm run preview
```

## ✅ Виправлені помилки

1. ✅ Оновлено всі відносні імпорти на аліаси
2. ✅ Додано аліаси для всіх директорій проекту
3. ✅ Створено ThemeContext для роботи зі сторінкою About
4. ✅ Виправлено імпорти в роутері
5. ✅ Додано ThemeProvider в App.jsx
6. ✅ Оновлено конфігурації vite.config.js та jsconfig.json

## 📝 Примітки

- Всі компоненти використовують CSS Modules (`.module.css`)
- Lazy loading застосовано для оптимізації завантаження сторінок About та 404
- Suspense з Spinner використовується для відображення під час завантаження lazy компонентів
